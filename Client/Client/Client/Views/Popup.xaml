<?xml version="1.0" encoding="UTF-8"?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Client.Views.Popup"   
             xmlns:pages ="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup">


    <StackLayout Margin="12"
                 Padding="24"
                 Spacing="24"
                 WidthRequest="250"
                 BackgroundColor="White"
                 HorizontalOptions="Center"
                 VerticalOptions="Center">
        
    <StackLayout>
            <Label Grid.Column="0" Grid.Row="1" Text="Please select the team member" FontSize="Medium"/>
            <Picker Grid.Column="1" Grid.Row="1" Title="--Select--" 
                    ItemsSource="{Binding ListOfEmployee}" 
                    ItemDisplayBinding="{Binding Name}"
                    SelectedItem="{Binding CurrentEmployee}"/>
        </StackLayout>

        <Button BackgroundColor="Gray"
                FontSize="20"
                Text="Add"
                TextColor="White"
                Command="{Binding AddMember}"
         />
    </StackLayout>

</pages:PopupPage>